# M0-T51 - Run 001 - Global visual system and Windows desktop release

## Task ID

M0-T51

## Run Number

001

## Date

2026-08-03

## Original Request / Codex Prompt Summary

Roll out the prewritten `GhostUITheme` across the named presentation scripts and UI-owning scene builders, remove duplicated UI/font helpers, apply the exact chapter proportions, retire WebGL in favour of a nine-scene Windows x64 player, update packaging and launcher behavior, run the requested checks, document every result honestly, and make no puzzle-logic or authored-data changes.

## Files Created

- `Assets/Presentation/Common/GhostUITheme.cs.meta` - generated by Unity on import; its GUID was not hand-authored or changed.
- `Docs/codex_runs/M0-T51_001_global_visual_system.md` - this run record.

## Files Modified

### Shared presentation theme and UI

- `Assets/Presentation/Common/GhostUITheme.cs`
- `Assets/Presentation/Act1IntentClassification/Act1IntentClassificationStaticPresenter.cs`
- `Assets/Presentation/Act1IntentClassification/Editor/Act1IntentClassificationPrototypeSceneBuilder.cs`
- `Assets/Presentation/Act2EntityExtraction/Act2EntityExtractionStaticPresenter.cs`
- `Assets/Presentation/Act2EntityExtraction/Act2EntityTokenDragView.cs`
- `Assets/Presentation/Act3DialogGraph/Act3DialogGraphStaticPresenter.cs`
- `Assets/Presentation/Act3DialogGraph/Editor/Act3DialogGraphPrototypeSceneBuilder.cs`
- `Assets/Presentation/Act4ConfidenceFallback/Act4ConfidenceStaticPresenter.cs`
- `Assets/Presentation/Act5TestingDebugging/Act5TestingStaticPresenter.cs`
- `Assets/Presentation/Act6BackendResponse/Act6BackendStaticPresenter.cs`
- `Assets/Presentation/Act6BackendResponse/Act6BackendCardDragView.cs`
- `Assets/Presentation/Act6VoicePipeline/Act6PipelineStaticPresenter.cs`
- `Assets/Presentation/Act6VoicePipeline/Act6PipelinePartDragView.cs`
- `Assets/Presentation/Act6VoicePipeline/FinalChapterConversationPresenter.cs`
- `Assets/Presentation/Story/Chapter0StoryPresenter.cs`
- `Assets/Presentation/Banter/AmbientBanterHook.cs`
- `Assets/Presentation/Banter/AmbientBanterPanel.cs`
- `Assets/Presentation/Banter/LilyChatWindow.cs`
- `Assets/Presentation/GhostAvatar/GhostFaceView.cs`
- `Assets/Presentation/Shell/LilyDialogueFrame.cs`
- `Assets/Presentation/Shell/Editor/GameShellSceneBuilder.cs`
- `Assets/Presentation/Shell/ShellReturnToHubOverlay.cs`
- `Assets/Presentation/Fundamentals/ChatbotFundamentalsPresenter.cs`

### Desktop build and deployment

- `Assets/Editor/GhostWebGLReleaseBuilder.cs`
- `Backend/src/server.ts`
- `Deployment/build-release.ps1`
- `Deployment/Installer/Ghost.iss`
- `Deployment/Launcher/Program.cs`
- `Deployment/test-installer.ps1`
- `Deployment/README.md`

### Documentation

- `Docs/CODE_WALKTHROUGH.md`

## Tests or Checks Run

1. `npm.cmd test` in `Backend/`.
2. `npm.cmd run build` in `Backend/`.
3. `dotnet build Deployment/Launcher/GhostLauncher.csproj -c Release`.
4. PowerShell parser checks for `Deployment/build-release.ps1` and `Deployment/test-installer.ps1`.
5. Unity batch compile checks with Unity 6000.4.11f1.
6. Chapter 1 scene-builder execution attempt through `Act1IntentClassificationPrototypeSceneBuilder.BuildAct1IntentClassificationPrototypeScene`.
7. Unity EditMode test-run attempt without `-quit`, writing to `Build/M0-T51-editmode-results.xml` if compilation succeeded.
8. Static YAML audit of all nine existing release scenes for `Main Camera`, `Canvas`, `EventSystem`, and `m_Script: {fileID: 0}`.
9. Presentation grep for local text/panel/font helpers and raw numeric font sizes.
10. Presentation grep for remaining `AddComponent<Image>()` calls.
11. Release-path grep for `WebGL`, `GHOST_WEB_ROOT`, `unity-container`, and browser-launch remnants.
12. `git diff --check`.

## Test / Check Result

- Backend tests: **10 passed / 10 total**, one test file passed.
- Backend TypeScript build: **passed**.
- Launcher build: **passed**, 0 warnings and 0 errors.
- PowerShell syntax: **passed** for both edited scripts.
- Unity compile: **failed** with 15 unique compiler-error lines caused by the current Act 4 model/controller API mismatch. Examples include missing `Act4ConfidenceDemoData.StartingThreshold`, `Act4ConfidenceConfiguration`, `Act4VisitorRunResult.ActualOutcome`, and `Act4VisitorMessage.ExpectedOutcome`. No M0-T51 theme, banter, shell-button, desktop-builder, or launcher C# compiler errors were reported.
- Scene regeneration: Not run — Unity compilation failed before the Chapter 1 builder method could execute. The remaining builders were not invoked because they would stop at the same compile gate.
- Existing-scene structural audit: all nine scene YAML files contain exactly 1 `Main Camera`, 1 `Canvas`, 1 `EventSystem`, and 0 null `m_Script` references. This is a static audit of the existing scenes, not a claim that they were regenerated in this run.
- Unity EditMode suite: Not run — Unity compilation failed before the test runner started; exit code 1 and no results XML was created. Therefore no Unity pass/fail count is claimed.
- Windows standalone player build: Not run — the same Unity compilation failure prevents `BuildPipeline.BuildPlayer` from running.
- 1920x1080 chapter screenshots: Not run — scenes could not enter a compiled Editor/Play Mode state.
- Installer and clean-environment package tests: Not run — no Windows player/release payload could be built.
- Duplicate helper/raw-size audit: no scoped local text/panel/font helper or raw numeric font-size assignment remains; the only `LegacyRuntime.ttf` to `Arial.ttf` fallback is the central `GhostUITheme.Font` implementation.
- WebGL reference audit across `Assets/Editor`, `Deployment`, and `Backend/src/server.ts`: no runtime WebGL/browser-serving reference remains.
- `git diff --check`: passed; Git emitted only existing line-ending conversion warnings.

### Remaining direct `Image` construction and justification

The final grep found 21 calls, all outside the rounded panel/card/chip/button/drop-zone category:

- Full-screen/backdrop graphics: `Chapter0StoryPresenter.cs:94`, `Act6BackendStaticPresenter.cs:129`, `Act6PipelineStaticPresenter.cs:173`, `FinalChapterConversationPresenter.cs:190`.
- Portrait and ending artwork: `GhostFaceView.cs:174`, `Act6PipelineStaticPresenter.cs:1457,1507`, `FinalChapterConversationPresenter.cs:432,446,1110,1160`.
- Graph/test visualization primitives: `Act3DialogGraphStaticPresenter.cs:1232,1251,1368`, `Act5TestingStaticPresenter.cs:700,959`.
- Slider visualization primitives: `Act4ConfidenceStaticPresenter.cs:662`, `FinalChapterConversationPresenter.cs:761,780,799`.
- The central rounded-surface factory itself: `GhostUITheme.cs:499`.

No remaining direct call constructs a gameplay panel, card, chip, button, or drop zone.

## Errors Encountered

- The provided patch helper could not read workspace files because the Windows sandbox helper returned `apply deny-read ACLs`. Exact-match, fail-fast PowerShell file writes were used instead, followed by focused greps, compiler checks, and diff checks.
- The first launcher `dotnet build --no-restore` failed because `obj/project.assets.json` did not exist. A normal build restored the project and then passed.
- Initial Unity compilation exposed missing `Ghost.Presentation.Common` imports in several migrated files. The imports were added and duplicate imports were collapsed to one per file.
- Unity then consistently stopped on the Act 4 API mismatch listed above. The mismatch is unrelated to UI factories and cannot be repaired within M0-T51 without changing presentation interaction behavior and puzzle-model contracts that the task explicitly excludes.

## Fixes Applied

- Replaced local text, fill-text, panel, button, font-resolution, and text-colour implementations with `GhostUITheme` calls.
- Added only compatibility overloads needed by existing geometry or existing GameObjects; no token value, colour, radius, or visual-design decision was changed.
- Applied `TitleSize`, `HeadingSize`, `BodySize`, `SmallSize`, `TinySize`, `Ink`, `InkSoft`, `Good`, and `Bad` mechanically according to the prompt.
- Applied 44 px headers, 40 px objective strips, 96 px minimum/flexible body regions, 12 px content padding, and 170 px conversation panels across chapter presenters.
- Renamed the Act 1 field to `pileList` while retaining serialized compatibility with `FormerlySerializedAs`.
- Converted the release builder to `StandaloneWindows64`, changed staging to `app/player`, changed the launcher from browser opening to native-player launching, removed backend WebGL serving, and updated installer checks/documentation.
- Migrated the two additional runtime button factories surfaced by the global bare-Image audit (`ShellReturnToHubOverlay` and the temporary fundamentals screen).

## What Was Intentionally Not Changed

- No validator, simulator, session model, authored puzzle data, `Packages/`, `ProjectSettings/`, or existing `.meta` GUID was edited for M0-T51.
- The release builder class is now `GhostDesktopReleaseBuilder`, but the source filename remains `GhostWebGLReleaseBuilder.cs` because repository rules prohibit renaming its existing `.meta` file. This avoids changing the asset GUID while still removing the WebGL build path.
- Direct Images used as backgrounds, portraits, slider parts, ports, wires, and glow artwork were retained because rounded widget factories are not appropriate for those graphics.
- Scene YAML was not hand-edited. The failed Unity builder attempt did not reach the scene-save step.
- `Docs/CURRENT_TASK.md`, `Docs/HANDOFF_LOG.md`, and completed-task archives were not advanced; task closure belongs to Claude after review and successful Unity verification.

## Remaining Risks

- Unity scene regeneration, EditMode regression counts, the Windows player build, and visual screenshot review remain unverified until the Act 4 API mismatch is resolved on a coherent project state.
- The source filename/class-name mismatch for the desktop builder is intentional for meta preservation, but a later approved asset rename should move both `.cs` and `.meta` together if repository policy changes.
- Static YAML counts do not replace opening regenerated scenes in Unity and checking the Console.
- The worktree contained extensive existing modified and untracked work. M0-T51 changes were kept to presentation, deployment, backend WebGL removal, and documentation; reviewers should inspect the full working tree before committing.

## Next Recommended Step

Have Claude reconcile or restore the coherent Act 4 model/controller set without folding that future gameplay work into M0-T51. Then rerun all nine scene builders, the EditMode suite without `-quit`, the Windows standalone build, four 1920x1080 screenshots including Chapters 1 and 4, and the staged/installed clean-environment tests before closing M0-T51.

## Chinese STAR Summary

- S情境：项目需要把分散在各章节中的字体、颜色、面板和按钮实现统一到既有的 `GhostUITheme`，并淘汰依赖浏览器的 WebGL 发布路径。
- T任务：完成全局视觉系统迁移、精确页面比例调整和 Windows 原生发布链改造，同时保持确定性谜题逻辑与已创作数据不变。
- A行动：迁移演示器与 UI 构建器、删除重复辅助函数、统一圆角控件与文字令牌、改造 Unity 构建器/启动器/安装脚本、删除后端 WebGL 服务代码，并执行后端、启动器、脚本、Unity 编译和静态场景检查。
- R结果：后端 10/10 测试通过，TypeScript 与启动器构建通过，脚本语法和静态场景结构检查通过；Unity 后续验证被当前 Act 4 API 不一致阻断，因此未虚报场景重建、EditMode 计数、桌面构建或截图结果。