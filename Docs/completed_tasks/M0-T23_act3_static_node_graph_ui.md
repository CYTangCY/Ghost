# M0-T23 — Act 3 Static Node-Graph UI Prototype Scene (display-only)

## Completion Status

Completed across Codex runs 001–003 (001 initial; 002 layout fix; 003 swapped a fragile ScrollRect for
compact static lists so palette/goal content stays visible). The user generated the scene and verified
it renders correctly (screenshot). Claude reviewed scope and the run logs.

## Date

2026-06-22

## Summary

Act 3 equivalent of M0-T16: a display-only node-graph scene generated by an Editor menu builder. The
presenter, backed by `DialogGraphSession.CreateFromSampleData()`, renders a node-type palette
(Start/IntentBranch/SlotCheck/Response) + level vocabulary (intent `find_object`, entity `room`,
responses `answer_object_location`/`ask_for_room`), an empty graph canvas region, a Goal/Tests panel
listing the target conversations, and a disabled placeholder Validate + feedback. No placement,
connection, or validation. Scene not added to Build Settings.

## Files Created

- `Assets/Presentation/Act3DialogGraph/Act3DialogGraphStaticPresenter.cs`
- `Assets/Presentation/Act3DialogGraph/Editor/Ghost.Presentation.Act3.Editor.asmdef`
- `Assets/Presentation/Act3DialogGraph/Editor/Act3DialogGraphPrototypeSceneBuilder.cs`
  (menu `Ghost > Build Act 3 Dialog Graph Prototype Scene`)
- `Docs/codex_runs/M0-T23_001_act3_static_node_graph_ui.md`,
  `Docs/codex_runs/M0-T23_002_act3_static_ui_layout_fix.md`,
  `Docs/codex_runs/M0-T23_003_act3_static_ui_list_visibility_fix.md`
- `Assets/Scenes/Act3DialogGraphPrototype.unity` (generated by the user via the menu builder)

## Files Modified

- `Docs/CODE_WALKTHROUGH.md`, `Docs/UNITY_TEST_CHECKLIST.md`

## Claude Review Notes

- Scope clean (git status): only the Act 3 presentation folder + generated scene + 3 run logs + 2 docs
  (+ Unity `.meta`); no Build Settings registration, no Act 3 pure logic / Act 1 / Act 2 / Game Shell
  edits, no existing asmdef changes. The one new Act 3 Editor asmdef was permitted.
- Display-only confirmed by the screenshot (palette, node types, vocabulary, empty canvas, goal/tests,
  disabled Validate). The 003 ScrollRect→compact-list change is layout-only.
- Run logs honest: dotnet build passed (Codex's own check); scene generation / Play Mode / Unity tests
  "Not run" in the Codex session. All three run logs retained.

## Human Verification Result

The user generated the scene via the builder and verified it renders the palette + node types + level
vocabulary + empty canvas + goal/tests + disabled Validate, with content visible inside its panels
(screenshot). Source of the "works" status.

## Remaining Risks

- Scene is a menu-builder artifact (rebuild if stale, as seen across the 001–003 layout iterations).
- Manual-in-Editor verification only.

## Next Task

M0-T24 — Act 3 node placement + connection interaction: place nodes from the palette, configure node
fields (intent / entity-slot / response), connect nodes with a transition condition, and set the start
node — all routed through `DialogGraphSession`. Validation feedback follows in M0-T25.
