# M0-T16 — Act 2 Static Span-Annotation UI Prototype Scene (display-only)

## Completion Status

Completed. Codex run 001 added the presenter, chip view, Editor scene builder, and the new Act 2
Editor asmdef. The user ran `Ghost > Build Act 2 Entity Extraction Prototype Scene`, generating
`Assets/Scenes/Act2EntityExtractionPrototype.unity`, and verified the rendered scene (screenshot).
Claude reviewed the code and scope.

## Date

2026-06-22

## Summary

Added the Act 2 equivalent of Act 1's M0-T07: a display-only span-annotation scene generated by an
Editor menu builder. The presenter renders one sample message as word chips (each carrying its
character `Start`/`Length` via `Act2EntityChipView`), an entity-type palette/legend (System/Custom),
and a disabled placeholder Validate button + feedback. No selection, assignment, or working
validation. The scene is not added to Build Settings.

## Files Created

- `Assets/Presentation/Act2EntityExtraction/Act2EntityChipView.cs`
- `Assets/Presentation/Act2EntityExtraction/Act2EntityExtractionStaticPresenter.cs`
- `Assets/Presentation/Act2EntityExtraction/Editor/Ghost.Presentation.Act2.Editor.asmdef` (allowed new Act 2 editor assembly)
- `Assets/Presentation/Act2EntityExtraction/Editor/Act2EntityExtractionPrototypeSceneBuilder.cs`
  (menu `Ghost > Build Act 2 Entity Extraction Prototype Scene`)
- `Docs/codex_runs/M0-T16_001_act2_static_span_annotation_ui.md`
- `Assets/Scenes/Act2EntityExtractionPrototype.unity` — generated by the user via the menu builder
  (Codex could not generate it in batch; verified present on disk during closure).

## Files Modified

- `Docs/CODE_WALKTHROUGH.md`
- `Docs/UNITY_TEST_CHECKLIST.md`

## Design / Behaviour

- Word-chip tokenizer trims surrounding non-alphanumeric characters, so the sample entities
  (`lab`, `9pm`, `laboratory`, `lantern`) each align to exactly one chip — the chip `Start`/`Length`
  match the authored `EntitySpan`s, readying the later interaction task.
- Each chip carries `Start`/`Length`/`Text` via `Act2EntityChipView` (verifiable in code).
- Entity palette built from `CreateTimeEntityType()/CreateRoomEntityType()/CreateObjectEntityType()`,
  coloured + labelled by System/Custom category.
- Validate is a disabled placeholder (no validation wiring); `EnsureEventSystem` mirrors Act 1.
- The scene is a menu-builder artifact (no hand-written `.unity` YAML) and is NOT in Build Settings.

## Claude Review Notes

- Scope: clean — only the Act 2 presentation folder + the user-generated scene + 2 doc updates + run
  log + the one permitted new Act 2 Editor asmdef; no Build Settings registration (scope scan +
  builder confirm); no Act 1 / Game Shell / pure-logic / `CURRENT_TASK.md` changes.
- Display-only confirmed: Validate `interactable = false`, listeners removed, no validation calls.
- Chip-offset readiness correct (punctuation trimmed).
- Minor (non-blocking): `Act2EntityChipView` uses public fields — fine for a small data-holder
  component.

## Codex Run Logs

- `Docs/codex_runs/M0-T16_001_act2_static_span_annotation_ui.md` — honest "Not run" for scene
  generation / Play Mode / tests (Unity unavailable in the Codex session); the user generated the
  scene and verified it.

## Human Verification Result

Performed by the user in the Unity Editor: ran the menu builder, generated the scene, and confirmed it
renders the title, the message as word chips (`Ghost / heard / humming / in / the / lab / at / 9pm`,
period trimmed), the entity-type legend (`time` System; `room`, `object` Custom), and the placeholder
Validate button + feedback, with no interaction (screenshot provided). This is the source of the
"renders correctly" status — not the Codex session.

## Remaining Risks

- Manual-in-Editor verification only; the scene is a menu-builder artifact (rebuild if it looks stale
  after script changes).
- Unity-generated `.meta` files and the `.unity` scene should be committed alongside the scripts;
  confirm the scene is not in Build Settings before committing.

## Next Task

M0-T17 — Act 2 chip selection + entity-type assignment interaction (click-based), wiring chips +
palette through `EntityExtractionSession`; mirrors Act 1's M0-T08. Validation feedback follows in
M0-T18.
